# Production Environment Variables Template
# Kopiere diese Datei zu .env.production und ersetze die Werte

# Database Configuration (Render.com PostgreSQL)
DATABASE_URL=postgresql://[DB_USER]:[DB_PASSWORD]@[DB_HOST]:[DB_PORT]/[DB_NAME]

# Application Configuration
NODE_ENV=production
PORT=10000

# Frontend Configuration (für .env.production im frontend/ Ordner)
VITE_SOCKET_URL=https://[your-backend-service].onrender.com

# Anweisungen:
# 1. Diese Datei NIEMALS mit echten Credentials committen!
# 2. Echte Werte nur in der Hosting-Platform konfigurieren
# 3. Für lokale Tests: lokale .env Dateien verwenden
# 4. Placeholder-Werte durch echte Werte ersetzen:
#    [DB_USER] → dein Datenbank-Benutzername
#    [DB_PASSWORD] → dein Datenbank-Passwort
#    [DB_HOST] → dein Datenbank-Host
#    [DB_PORT] → dein Datenbank-Port (meist 5432)
#    [DB_NAME] → dein Datenbank-Name
#    [your-backend-service] → dein Render.com Service-Name
